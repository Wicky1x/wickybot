const { Client } = require("discord.js");

module.exsports = async (client) =>{
    const guild = client.guilds.cache.get('786628025054789702');
    setInterval(() =>{
        const memberCount = guild.memberCount;
    });
}